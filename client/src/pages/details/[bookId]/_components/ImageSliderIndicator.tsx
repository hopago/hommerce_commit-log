type ImageSliderIndicatorProps = {
  handleNext: () => void;
  handlePrev: () => void;
};

export default function ImageSliderIndicator({
  handleNext,
  handlePrev,
}: ImageSliderIndicatorProps) {
  return <div>ImageSliderIndicator</div>;
}
