type ImageHoverMenuProps = {
  setShow: React.Dispatch<React.SetStateAction<boolean>>;
};

export default function ImageHoverMenu({ setShow }: ImageHoverMenuProps) {
  return <div>ImageHoverMenu</div>;
}
